# Rust / Tauri
app/target/
app/gen/

# Node
node_modules/

# 本机工具链配置（含个人 MinGW 路径，勿提交；模板见 config.example.toml）
app/.cargo/config.toml

# 设计稿（开发期工作产物，不随仓库发布）
design/

# 临时
*.log
.playwright-cli/
tmp/

# WorkBuddy 会话本地数据
.workbuddy/

# 本地打包产物（免安装目录/zip，随版本变化，不入库）
dist/
